name: R5_high_concurrency
regime_hint: saturation_high_conc
source:
  generated_by: regime_study_2026_06_01
description: |
  R5 — high concurrency (saturation regime). NOTE: server config has
  max-running-requests=32; this regime intentionally requests 64 concurrent
  to expose the cap behavior.
dataset:
  name: random
  random_input_len: 512
  random_output_len: 256
  random_range_ratio: 0.0
traffic:
  max_concurrency: 64
  num_prompts: 128
cache:
  mode: cold
  flush_cache: true
seed: 1234
